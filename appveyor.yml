branches:
  except:
    - gh-pages
    
# Do not build on tags (GitHub only)
skip_tags: true

build:
  project: Duality.sln
  verbosity: minimal
  
test:
  assemblies:
    - 'UnitTesting\*.dll'
  categories:
    except:
      - Rendering   # AppVeyor VMs don't have GPU access.
